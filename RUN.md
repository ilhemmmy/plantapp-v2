# Run commands

## Install

```powershell
npm install
```

## Start Expo

```powershell
npm run start
```

## Shortcuts (Expo terminal)

- Press `a` for Android
- Press `w` for Web
- Press `r` to reload
- Press `m` to toggle menu
